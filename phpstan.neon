includes:
  - vendor/szepeviktor/phpstan-wordpress/extension.neon

parameters:
  level: 4
  paths:
    - .
  excludePaths:
    - vendor/*
    - node_modules/*
    - .github/*

  # teach PHPStan WP function signatures
  stubFiles:
    - vendor/php-stubs/wordpress-stubs/wordpress-stubs.php

  # (Optional) define common WP constants so checks don’t complain
  # constants:
  #   ABSPATH: ''
